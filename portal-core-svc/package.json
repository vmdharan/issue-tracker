{
  "name": "portal-core-svc",
  "version": "0.0.1",
  "description": "Server for the issue-tracker web portal",
  "main": "server.js",
  "scripts": {
    "server": "nodemon",
    "client": "npm start --prefix ../portal",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "vmdharan",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "config": "^3.3.9",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.4.4"
  }
}
